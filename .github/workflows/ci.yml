name: CI

on:
  push:
  pull_request:

jobs:
  checks:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: rca_ci
          POSTGRES_PASSWORD: isolated-ci-only
          POSTGRES_DB: rca_ci
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U rca_ci -d rca_ci"
          --health-interval 5s --health-timeout 5s --health-retries 10
    env:
      RCA_POSTGRES_TEST_URL: postgresql+asyncpg://rca_ci:isolated-ci-only@127.0.0.1:5432/rca_ci
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Install OCR runtime
        run: sudo apt-get update && sudo apt-get install -y --no-install-recommends tesseract-ocr postgresql-client-16
      - run: uv sync --locked --extra dev
      - run: uv run ruff check .
      - run: PATH="/usr/lib/postgresql/16/bin:$PATH" uv run python -m pytest -v
      - run: make smoke
      - run: npm ci
        working-directory: frontend
      - run: npm run lint
        working-directory: frontend
      - run: npm test
        working-directory: frontend
      - run: docker build --tag rca-analyzer:ci .
